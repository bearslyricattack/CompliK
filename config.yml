plugins:
  - name: "cron"
    type: "scheduler"
    enabled: true
    settings: |
      {
        "test": "测试11111111"
      }

  - name: "website"
    type: "output"
    enabled: true
    settings: '{}'

  - name: "result"
    type: "output"
    enabled: true
    settings: '{}'

logging:
  level: "info"

kubeconfig: "kubeconfig.yml"