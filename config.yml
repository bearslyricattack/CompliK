plugins:
  - name: "Cron"
    type: "Discovery"
    enabled: true
    settings: |
      {
        "test": "测试11111111"
      }

  - name: "Website"
    type: "Compliance"
    enabled: true
    settings: '{}'

  - name: "Database"
    type: "Handle"
    enabled: true
    settings: '{}'

  - name: "Lark"
    type: "Handle"
    enabled: true
    settings: '{}'

logging:
  level: "info"

kubeconfig: "io.yml"