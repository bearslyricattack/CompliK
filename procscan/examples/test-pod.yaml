# =============================================================================
# 🧪 ProcScan 测试Pod配置
# =============================================================================

apiVersion: v1
kind: Pod
metadata:
  name: test-malicious-pod
  namespace: procscan-debug
  labels:
    app: test-malicious
spec:
  containers:
  - name: test-malicious-container
    image: busybox:1.35
    # 模拟恶意进程 - 这会触发关键词检测
    command: ["/bin/sh", "-c"]
    args:
      - |
        echo "Starting fake-miner process for testing..."
        echo "Mining cryptocurrency with stratum+tcp://pool.example.com:4444"
        while true; do
          # 模拟挖矿进程
          echo "test-malware process running"
          sleep 30
        done
    resources:
      requests:
        memory: "64Mi"
        cpu: "50m"
      limits:
        memory: "128Mi"
        cpu: "100m"
  restartPolicy: Always

---
# 另一个测试Pod - 模拟正常进程（应该在白名单中）
apiVersion: v1
kind: Pod
metadata:
  name: test-normal-pod
  namespace: procscan-debug
  labels:
    app: test-normal
spec:
  containers:
  - name: test-normal-container
    image: nginx:1.21
    ports:
    - containerPort: 80
    resources:
      requests:
        memory: "64Mi"
        cpu: "50m"
      limits:
        memory: "128Mi"
        cpu: "100m"
  restartPolicy: Always